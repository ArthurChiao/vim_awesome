Setting Vim in Source File
==========================

1. Python

  To set file type to python for file `script.vpy`, add following line to the
  start of the file:

  ```shell
  # vim: set filetype=python
  ```

  Then the file content will be highlighted in Python format.
